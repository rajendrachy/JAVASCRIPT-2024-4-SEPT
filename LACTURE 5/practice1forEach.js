//for given array of the number , print the square of each value using forEach lopp


let nums = [2, 3, 4, 5, 6];

nums.forEach((nums) => { // nums or num
    console.log(nums * nums); //or num**2
});